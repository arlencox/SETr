<*> or <sdsl/*> or <strace/*>: use_SETr, package(unix), package(str), package(zarith)
#ifdef PKG_CUDD
<*> or <sdsl/*> or <strace/*>: package(cudd)
#endif
#ifdef PKG_Z3
<*> or <sdsl/*> or <strace/*>: package(Z3)
#endif
#ifdef PKG_MLBDD
<*> or <sdsl/*> or <strace/*>: package(mlbdd)
#endif
<sdsl/*>: for-pack(SDSL)
<strace/*>: for-pack(STrace)

